[project]
name = "varvaluation"
version = "0.2.0"
description = "Discount curves and present values from one VAR on a Polars state frame"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [{ name = "Thomas Lorans" }]
keywords = [
    "valuation",
    "VAR",
    "discount-rate",
    "cash-flow",
    "asset-pricing",
    "polars",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business :: Financial",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "numpy>=1.26",
    "scipy>=1.11",
    "polars>=1.0",
    "pandera>=0.20",
    "statsmodels>=0.14",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.6"]
docs = ["mkdocs>=1.6", "mkdocs-material>=9.5", "matplotlib>=3.7"]
data = [
    "pandas-datareader>=0.10",
    "pyarrow>=14",
    "openpyxl>=3.1",
    "python-dotenv>=1.0",
]
wrds = [
    "wrds>=3.2",
    "python-dotenv>=1.0",
    "pandas>=2.0",
    "pyarrow>=14",
]

[project.urls]
Homepage = "https://tlorans.github.io/varvaluation/"
Documentation = "https://tlorans.github.io/varvaluation/"
Repository = "https://github.com/tlorans/varvaluation"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/varvaluation"]

[tool.uv]
package = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "wrds: live WRDS queries (skipped without credentials)",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
